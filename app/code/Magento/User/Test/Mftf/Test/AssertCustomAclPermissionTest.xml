<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AssertAdminCustomAclPermissionTest">
        <annotations>
            <features value="User"/>
            <stories value="Assert Custom Variable Restricted Access"/>
            <title value="Check that access to variables block on CMS page in admin panel is restricted"/>
            <description value="Check that access to variables block on CMS page in admin panel is restricted"/>
            <group value="user"/>
            <group value="mtf_migrated"/>
        </annotations>

        <before>
            <executeJS function="return '{{DefaultAdminUser.password}}'" stepKey="adminPassword" />
            <createData entity="NewAdminUser" stepKey="user">
                <field key="current_password">{$adminPassword}</field>
            </createData>
            <createData entity="adminWithoutVariableRole" stepKey="user_role">
                <field key="current_password">{$adminPassword}</field>
            </createData>
            <actionGroup ref="LoginAsAdmin" stepKey="logIn"/>
        </before>
        <after>
            <actionGroup ref="LoginAsAdmin" stepKey="LoginAdmin"/>
            <actionGroup ref="AdminDeleteRoleByRoleNameActionGroup" stepKey="deleteRole">
                <argument name="role" value="$$user_role$$"/>
            </actionGroup>
            <actionGroup ref="AdminDeleteUserViaCurlActionGroup" stepKey="deleteUser">
                <argument name="user" value="$$user$$" />
            </actionGroup>
            <actionGroup ref="logout" stepKey="logOut"/>
        </after>

        <!--Assign new role-->
        <actionGroup ref="AdminOpenUserEditPageActionGroup" stepKey="openUserEditPage">
            <argument name="user" value="$$user$$"/>
        </actionGroup>
        <actionGroup ref="AdminUpdateUserRoleUsingCustomAdminPasswordActionGroup" stepKey="assignNewUserRole">
            <argument name="roleName" value="$$user_role.rolename$$"/>
        </actionGroup>
        <actionGroup ref="AdminClickSaveButtonOnUserFormActionGroup" stepKey="saveUser"/>

        <!--LogIn as admin user with new role-->
        <actionGroup ref="logout" stepKey="logOutAsDefaultAdminUser"/>
        <actionGroup ref="LoginAsAdmin" stepKey="loginAsNewUser">
            <argument name="adminUser" value="$$user$$"/>
        </actionGroup>

        <!--Assert Custom Restricted Access-->
        <actionGroup ref="AdminOpenCreateNewCMSPageActionGroup" stepKey="openNewCmsPage"/>
        <actionGroup ref="AdminExpandContentSectionOnCmsPageActionGroup" stepKey="openContentSection"/>
        <actionGroup ref="AdminPressInsertVariableButtonActionGroup" stepKey="pressInsertVariableButton"/>
        <actionGroup ref="AssertAdminCustomVariableRestrictedAccessActionGroup" stepKey="seeErrorMessage"/>
        <actionGroup ref="logout" stepKey="logOut"/>
    </test>
</tests>
