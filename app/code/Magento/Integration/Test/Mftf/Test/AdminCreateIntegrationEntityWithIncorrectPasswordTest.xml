<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="AdminCreateIntegrationEntityWithIncorrectPasswordTest">
        <annotations>
            <features value="Integration"/>
            <stories value="System Integration with incorrect password"/>
            <title value="Admin system integration with incorrect password"/>
            <description value="Admin Create Integration with incorrect password"/>
            <group value="integration"/>
            <group value="mtf_migrated"/>
        </annotations>
        <before>
            <actionGroup ref="AdminLoginActionGroup" stepKey="login"/>
        </before>
        <after>
            <actionGroup ref="AdminLogoutActionGroup" stepKey="logout"/>
        </after>

        <actionGroup ref="AdminOpenNewIntegrationPageActionGroup" stepKey="navigateToNewIntegrationsPage"/>
        <actionGroup ref="AdminFillExtraFieldsForIntegrationFormActionGroup" stepKey="createNewIntegration">
            <argument name="name" value="{{defaultIntegrationData.name}}"/>
            <argument name="password" value="admin_user_incorrect_password"/>
            <argument name="email" value="test@example.com"/>
            <argument name="endpoint" value="https://endpoint.com"/>
            <argument name="linkUrl" value="https://testlink.com"/>
        </actionGroup>
        <actionGroup ref="AdminSubmitNewIntegrationFormActionGroup" stepKey="submitTheForm"/>

        <!-- Assert Incorrect User Password -->
        <actionGroup ref="AssertAdminMessageCreateIntegrationEntityActionGroup" stepKey="seeErrorMessage">
            <argument name="message" value="The password entered for the current user is invalid. Verify the password and try again."/>
            <argument name="messageType" value="error"/>
        </actionGroup>

        <!-- Assert Integration Is Not In Grid -->
        <actionGroup ref="AdminOpenIntegrationGridPageActionGroup" stepKey="goToIntegrationGrid"/>
        <actionGroup ref="AdminSearchIntegrationInGridActionGroup" stepKey="searchIntegration">
            <argument name="name" value="{{defaultIntegrationData.name}}"/>
        </actionGroup>
        <actionGroup ref="AssertDeletedIntegrationIsNotInGridActionGroup" stepKey="dontSeeIntegration">
            <argument name="name" value="{{defaultIntegrationData.name}}"/>
        </actionGroup>
    </test>
</tests>
